{
   "name": "lms-project",
   "version": "1.0.0",
   "scripts": {
      "dev": "concurrently --names BACKEND,FRONTEND --prefix-colors cyan,magenta \"npm --prefix Backend run dev\" \"npm --prefix client run dev\"",
      "backend": "npm --prefix Backend run dev",
      "frontend": "npm --prefix client run dev"
   },
   "devDependencies": {
      "concurrently": "^8.2.2"
   }
}
