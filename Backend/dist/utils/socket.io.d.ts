import { Server as HTTPServer } from 'http';
import { Server } from 'socket.io';
declare function initSocket(server: HTTPServer): Server;
export default initSocket;
//# sourceMappingURL=socket.io.d.ts.map